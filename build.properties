### Do not edit this; override in local.build.properties if you must

src.dir=${basedir}/src

java.src.dir=${src.dir}/java

sh.src.dir=${src.dir}/sh

perl.src.dir=${src.dir}/perl

# Ant includes
ant.src.dir=${src.dir}/ant

test.src.dir=${basedir}/test/src

test.java.src.dir=${test.src.dir}/java

lib.dir=${basedir}/lib

# Libs needed at runtime
rt.lib.dir=${lib.dir}/rt

# Libs needed at test time
test.lib.dir=${lib.dir}/test

build.dir=${basedir}/build

javadoc.dir=${build.dir}/doc/javadoc

classes.dir=${build.dir}/classes

emma.enabled=true

emma.dir=${build.dir}/emma

emma.classes.dir=${emma.dir}/classes

emma.coverage.dir=${emma.dir}/coverage

test.classes.dir=${build.dir}/test-classes

test.output.dir=${build.dir}/test-output

conf.dir=${basedir}/conf

test.project.dir=${basedir}/test/test_project

test.project.src.dir=${test.project.dir}/src/java

test.project.classes.dir=${test.project.dir}/build/classes

test.source.file=net/sourceforge/importscrubber/FunctionalTest.java
